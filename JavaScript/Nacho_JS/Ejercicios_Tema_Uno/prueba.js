
let intervalos = [
    [1,3],
    [2,6],
    [10,12]
];

function merge(intervalos) {

    intervalos.sort((a, b) => a[0] - b[0]);

    const resultado = [];
    let intervaloActual = intervalos[0];

    for (let i = 0; i < intervalos.length; i++)
    {

    }

    return ""
}

console.log(merge(intervalos));